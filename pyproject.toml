[project]
name = "jijmodeling-tutorial"
version = "1.8.0"
description = "Tutorial for JijModeling"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "jijmodeling >= 1.8.0, < 1.9.0",
    "jupyterlab >= 4.2.5",
    "jupyterlab-rise>=0.42.0",
    "ommx >= 1.3.0, < 2.0.0",
    "ommx-python-mip-adapter >= 1.3.2, < 2.0.0",
]
